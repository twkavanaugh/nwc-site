---
import Layout from "../layouts/Layout.astro";
import { BUSINESS } from "../data/business";
import { getCollection } from "astro:content";
import { getGrowGroupCount } from "../lib/grow-groups";
import { isUpcoming, byStartDateAsc, formatMonth, formatDay } from "../lib/date";
// Live count from the Grow Groups directory (staff-edited in Pages CMS) — was a hardcoded "12".
const growGroupCount = await getGrowGroupCount();

// Homepage "Upcoming" — top 3 upcoming events from the collection, same date
// filter/sort as /events. Replaces the old hardcoded, unsorted, yearless list.
const upcomingHome = (await getCollection("events"))
  .filter(isUpcoming)
  .sort(byStartDateAsc)
  .slice(0, 3);

// Homepage — Pass 1 of 3: Overlay hero, Service Times band.
// (Marquee/ticker strip REMOVED 2026-09-23 — the church did not like it.)
// Translated from brand/prototype/page-home.jsx (HeroOverlay ~57, ServiceTimes
// ~158) + shell.jsx (Marquee) + styles.css (.marquee). React/onNav stripped;
// onNav buttons are now real <a href> per the route map.

// Worship service times derive from BUSINESS.serviceTimes (single source of truth).
// Service band: first time without meridiem · second time full -> "9:00 · 10:45 AM".
const [svcTime1, svcTime2] = BUSINESS.serviceTimes;
const svcTime1NoMeridiem = svcTime1.replace(/\s*(AM|PM)$/i, "");

---

<Layout
  title="North Wake Church"
  description="North Wake Church is a family of faith in Wake Forest, NC — gathering each Sunday to worship Jesus and scattering through the week to love our neighbors near and far."
>
  <!-- ===================== HERO — "Scene + People" (devotional) ===================== -->
  <!-- A) Watercolor devotional hero — the place -->
  <section class="shero">
    <div class="shero-stage">
      <div class="shero-bg" aria-label="Watercolor of downtown Wake Forest"></div>
      <!-- Cream legibility scrim over the watercolor (page-local literal exception:
           real-image scrim, not a theme color — cf. --shadow-dropdown precedent). -->
      <div class="shero-scrim"></div>

      <div class="wrap-narrow shero-content">
        <div class="eyebrow fade-up shero-eyebrow">
          <span class="dot"></span>A family of faith · <span class="shero-eyebrow-place">Wake Forest, NC</span>
        </div>
        <h1 class="serif fade-up shero-title" style="animation-delay: 60ms;">
          A place to <span class="em-accent">know</span>, <span class="em-accent">grow</span>, and <span class="em-accent">go</span> in love.
        </h1>
        <p class="serif fade-up shero-lede" style="animation-delay: 140ms;">
          Right here near downtown Wake Forest — gathering each Sunday to worship Jesus,
          and scattering through the week to love one another and our neighbors near and far.
        </p>
        <div class="shero-actions fade-up" style="animation-delay: 200ms;">
          <a class="btn btn-primary" href="/visit">Plan a visit <span class="arr">→</span></a>
          <a class="btn btn-ghost" href="/sermons">Watch Sunday's sermon</a>
        </div>
      </div>
    </div>
  </section>

  <!-- B) Transition — serif line carries the eye from the place to the people -->
  <section class="shero-transition-sec">
    <div class="wrap-narrow shero-transition">
      <div class="shero-rule" aria-hidden="true"></div>
      <p class="serif shero-transition-line">
        The gospel creates a human community radically different from any society around it.
      </p>
    </div>
  </section>

  <!-- C) The people — framed 21:9 photo with a quiet caption -->
  <section class="shero-people-sec">
    <div class="wrap">
      <div class="shero-people">
        <!-- STOCK PLACEHOLDER (Unsplash, luis-morera) — replace with the real
             Sunday-gathering photo per docs/PHOTOGRAPHER-SHOT-LIST.md. -->
        <div class="shero-people-photo" aria-label="People gathered at North Wake Church"></div>
        <!-- Caption scrim (page-local literal exception: photo-darkening gradient). -->
        <div class="shero-people-caption">
          <div class="serif shero-people-caption-text">One family, gathered around the gospel.</div>
        </div>
      </div>
    </div>
  </section>

  <!-- ===================== SERVICE TIMES BAND ===================== -->
  <section class="service">
    <div class="wrap">
      <div class="service-band">
        <div>
          <div class="mono service-eyebrow">Sundays</div>
          <div class="display-s service-time">{svcTime1NoMeridiem} <span class="sep-dot">·</span> {svcTime2}</div>
        </div>
        <div class="service-loc">
          <div class="mono service-eyebrow">Location</div>
          <div class="title service-addr">{BUSINESS.streetAddress} · {BUSINESS.city}, {BUSINESS.state}</div>
          <div class="small service-note">Free parking · Kids check-in opens 30 min before service</div>
        </div>
        {/* TODO: "Get directions" has no internal route; wire to a maps link once
            the address is verified. Inert for now (matches prototype's no-op). */}
        <button type="button" class="btn btn-ghost service-cta">
          Get directions <span class="arr">→</span>
        </button>
      </div>
    </div>
  </section>

  <!-- ===================== MISSION TRIAD ===================== -->
  <section class="mission">
    <div class="wrap">
      <div class="section-head">
        <div>
          <div class="eyebrow head-eyebrow"><span class="dot"></span>Why we exist</div>
          <h2 class="display-m mission-head">Three words help to shape what we do</h2>
        </div>
        <p class="body right">
          Our mission isn't a slogan — it's the orbit our church moves in. Sunday
          gathering, weekday community, sent into Wake Forest and beyond.
        </p>
      </div>
      <div class="mission-grid">
        {
          [
            // STOCK PLACEHOLDERS (Unsplash) — replace with real photography per
            // docs/PHOTOGRAPHER-SHOT-LIST.md; alt text is provisional.
            { verb: "Know", headline: "the love of God in Jesus Christ.", body: "We orient every Sunday and every week around the gospel — that God meets us in Jesus, and that meeting changes everything.", img: "/mission-know.jpg", alt: "People gathered in worship" },
            { verb: "Grow", headline: "in love for one another.", body: "We don't grow in isolation. In Grow Groups, classes, and shared meals, we open Scripture, pray honestly, and bear each other's burdens.", img: "/mission-grow.jpg", alt: "A small group gathered together" },
            { verb: "Go", headline: "in love to reach our neighbors near and far.", body: "From Wake Forest to the nations, we send and serve — through local outreach, mercy ministry, and global missions.", img: "/mission-go.jpg", alt: "Serving the local community" },
          ].map((it, i) => (
            <article class="mission-item">
              <img class="mission-photo" src={it.img} alt={it.alt} loading="lazy" />
              <div class="mission-num-row">
                <span class="mono mission-num">0{i + 1}</span>
                <span class="display-s mission-verb">{it.verb}</span>
              </div>
              <h3 class="title mission-headline">{it.headline}</h3>
              <p class="body mission-body">{it.body}</p>
            </article>
          ))
        }
      </div>
    </div>
  </section>

  <!-- ===================== PLAN-VISIT TEASER ===================== -->
  <section class="visit">
    <div class="wrap">
      <div class="visit-grid">
        <div class="visit-aside">
          <div class="eyebrow head-eyebrow"><span class="dot"></span>Plan a visit</div>
          <h2 class="display-m visit-title">What to expect on Sunday.</h2>
          <p class="lede visit-lede">
            Walking into a new church can feel like a lot. Here's exactly what your
            first Sunday at North Wake will look like — no surprises.
          </p>
          <div class="visit-actions">
            <a class="btn btn-primary" href="/visit">I'm new — start here <span class="arr">→</span></a>
          </div>
          <div class="visit-ph">
            <!-- STOCK PLACEHOLDER (Unsplash, alex-gallegos) — replace with a real
                 Welcome Wall / walk-up photo per docs/PHOTOGRAPHER-SHOT-LIST.md;
                 alt is provisional. Deliberately SHORT (21:9) band. -->
            <img
              class="visit-photo"
              src="/welcome-STOCK-PLACEHOLDER.jpg"
              alt="Two people embracing in greeting at church"
              loading="lazy"
            />
          </div>
        </div>
        <div>
          {
            [
              { k: "01", h: "Come as you are", b: "Jeans, a suit, somewhere in between — we don't dress to impress, we come to worship." },
              { k: "02", h: "Find a friendly face", b: "Grab a coffee at the Welcome Wall. Our team is there to help you find your seat (and your people)." },
              { k: "03", h: "Worship together", b: "Songs, prayer, and a sermon that takes the Bible seriously and your questions seriously, too." },
              { k: "04", h: "Kids are well cared for", b: "Safe, joyful, gospel-centered programming for birth through 5th grade in both services." },
            ].map((p) => (
              <div class="visit-point">
                <span class="mono visit-k">{p.k}</span>
                <div>
                  <h3 class="display-s visit-h">{p.h}</h3>
                  <p class="body visit-b">{p.b}</p>
                </div>
              </div>
            ))
          }
        </div>
      </div>
    </div>
  </section>

  <!-- ===================== FIND COMMUNITY ===================== -->
  <section class="community">
    <div class="wrap">
      <div class="section-head">
        <div>
          <div class="eyebrow head-eyebrow"><span class="dot"></span>Find community</div>
          <h2 class="display-m community-head">North Wake is a people, not a building.</h2>
        </div>
        <p class="body right">
          Sunday is a doorway, but life happens around the table. Find your people —
          a Grow Group, a class, a ministry that fits the season you're in.
        </p>
      </div>

      <div class="hairline-grid community-grid">
        {
          [
            { name: "Grow Groups", meta: `${growGroupCount} active groups · weekly`, body: "Mid-sized groups meeting in homes across Wake Forest. Share a meal, open Scripture, pray honestly, repeat.", href: "/community/grow-groups" },
            { name: "Adult Discipleship", meta: "8-week classes · Sundays 9 AM", body: "Theology and life classes for every season — from foundations of the faith to parenting, marriage, and money.", href: "/community/adult-discipleship" },
            { name: "Students", meta: "6th–12th · Wednesdays 6:30 PM", body: "A ministry for students and families to follow Jesus together — rooted in Colossians 1: presenting everyone mature in Christ.", href: "/community/students" },
            { name: "Kids", meta: "Birth–5th · Sunday mornings", body: "A safe, joyful place where kids hear that Jesus loves them and learn to love him back. Check-in 30 min before service.", href: "/community/kids" },
            { name: "Women", meta: "Bible studies · retreats", body: "Studies in the Word, friendships across generations, and gatherings that point women to the steadfast love of Christ.", href: "/community/women" },
            { name: "Men", meta: "Breakfast · book studies", body: "Men sharpening men — meeting over coffee and Scripture to grow into faithful husbands, fathers, and friends.", href: "/community/men" },
          ].map((g) => (
            <a class="community-card" href={g.href}>
              <div class="mono community-meta">{g.meta}</div>
              <h3 class="display-s community-name">{g.name}</h3>
              <p class="body community-body">{g.body}</p>
              <div class="community-cta">
                <span class="small label">Learn more</span>
                <span class="row-arr">→</span>
              </div>
            </a>
          ))
        }
      </div>

      <div class="community-all">
        <a class="btn btn-ghost" href="/community/grow-groups">See all ministries <span class="arr">→</span></a>
      </div>
    </div>
  </section>

  <!-- ===================== LATEST SERMON ===================== -->
  <section class="latest">
    <div class="wrap">
      <div class="section-head">
        <div>
          <div class="eyebrow head-eyebrow"><span class="dot"></span>Now teaching</div>
          <h2 class="display-m latest-head">Our Current Study Series:</h2>
        </div>
        <p class="body right">
          We're teaching through a book of the Bible each season. Listen along, or browse
          the full archive by speaker, series, or book.
        </p>
      </div>

      <a class="sermon-card" href="/sermons">
        <div class="sermon-art">
          <!-- STOCK PLACEHOLDER (Unsplash, jon-tyson — same image as the /sermons
               hero) — replace with real series artwork / photography per
               docs/PHOTOGRAPHER-SHOT-LIST.md; alt is provisional. -->
          <img
            class="sermon-art-img"
            src="/sermons-hero-STOCK-PLACEHOLDER.jpg"
            alt="A blue flag with a white dove and the word Peace"
            loading="lazy"
          />
        </div>
        <div class="sermon-body">
          <div class="mono sermon-kicker">Current series · Spring 2026</div>
          <h3 class="display-m sermon-title">Be Different — a journey through Romans.</h3>
          <p class="body sermon-lede">
            Paul's letter to the Romans, taught week by week — the gospel that
            re-orders everything: how we relate to God, to one another, and to a
            watching world.
          </p>
          <div class="sermon-foot">
            <span class="mono sermon-listen">Listen to the latest message →</span>
            {/* Card-level link already targets /sermons; this is a styled span,
                not a nested anchor (invalid HTML). */}
            <span class="btn btn-ghost">Sermon archive</span>
          </div>
        </div>
      </a>
    </div>
  </section>

  <!-- ===================== CARE (dark band) ===================== -->
  <section class="care">
    <div class="wrap">
      <div class="section-head">
        <div>
          <div class="eyebrow head-eyebrow care-eyebrow"><span class="dot"></span>Find help</div>
          <h2 class="display-m care-title">When life is hard, you don't walk alone.</h2>
        </div>
        <p class="body right care-note">
          North Wake operates four ministries that exist to serve the wounded, the
          hungry, and the worn-out — members and neighbors alike.
        </p>
      </div>

      <div class="care-grid">
        {
          [
            { name: "Hope Counseling Center", tag: "Counseling", body: "Christ-centered, Scripture-driven biblical counseling for the church and the community.", href: "/help/hope-counseling" },
            { name: "Mercy Health Clinic", tag: "Health", body: "Free and low-cost medical care offered as a tangible expression of God's love.", href: "/help/mercy-clinic" },
            { name: "Feed Ministry", tag: "Food pantry", body: "More than groceries — meaningful conversations, prayer, and witness alongside material help.", href: "/help/feed" },
            { name: "Care Ministry", tag: "Care", body: "Meals, visits, and presence for members walking through illness, grief, and hard seasons.", href: "/help/care" },
          ].map((c) => (
            <a class="care-card" href={c.href}>
              <span class="mono care-tag">{c.tag}</span>
              <h3 class="title care-name">{c.name}</h3>
              <p class="small care-body">{c.body}</p>
              <div class="care-cta">
                <span>Learn more</span>
                <span>→</span>
              </div>
            </a>
          ))
        }
      </div>
    </div>
  </section>

  <!-- ===================== EVENTS + WHO IS JESUS ===================== -->
  <section class="events">
    <div class="wrap">
      <div class="events-grid">
        <div>
          <div class="eyebrow head-eyebrow"><span class="dot"></span>What's next</div>
          <h2 class="display-m events-title">Events.</h2>
          <div class="events-list">
            {
              upcomingHome.length === 0 ? (
                <p class="events-empty">No events right now — check back soon.</p>
              ) : (
                upcomingHome.map((e) => (
                  <a class="row-link event-row" href={`/events/${e.id}`}>
                    <div class="event-left">
                      <div class="event-date">
                        <div class="mono event-month">{formatMonth(e.data.startDate)}</div>
                        <div class="display-s event-day">{formatDay(e.data.startDate)}</div>
                      </div>
                      <div>
                        <div class="title event-name">{e.data.title}</div>
                        {(e.data.time ?? e.data.subtitle) && (
                          <div class="small">{e.data.time ?? e.data.subtitle}</div>
                        )}
                      </div>
                    </div>
                    <span class="row-arr event-arr">→</span>
                  </a>
                ))
              )
            }
          </div>
          <div class="events-actions">
            <a class="btn btn-ghost" href="/events">Full calendar <span class="arr">→</span></a>
          </div>
        </div>

        <aside class="jesus-aside">
          <div class="eyebrow head-eyebrow jesus-eyebrow"><span class="dot"></span>For the seeker</div>
          <h3 class="display-m jesus-title">Who is Jesus?</h3>
          <p class="body jesus-body">
            Maybe you're not a Christian. Maybe you used to be. Maybe you've never
            thought about it seriously. This is a page written for honest questions —
            no jargon, no pressure.
          </p>
          <div class="jesus-actions">
            <a class="btn jesus-btn" href="/who-is-jesus">Read it <span class="arr">→</span></a>
          </div>
        </aside>
      </div>
    </div>
  </section>
</Layout>

<style>
  /* ============ HERO — "Scene + People" (devotional) ============ */
  /* A) Watercolor devotional hero */
  .shero {
    padding: 0;
  }
  .shero-stage {
    position: relative;
    width: 100%;
    min-height: 76vh;
    display: flex;
    overflow: hidden;
  }
  .shero-bg {
    position: absolute;
    inset: 0;
    background-image: url("/wake-forest-bcg.png");
    background-size: cover;
    background-position: center 60%;
    /* DOCUMENTED page-local literal: watercolor-paper fallback behind the image,
       only visible if it fails to load. Nearest raw token is --raw-sand-50
       (#f6f3ed) but it's tonally distinct (cooler) and serves a different role
       (page bg); the doc's #f3ecdf matches the watercolor's paper tone. */
    background-color: #f3ecdf;
  }
  /* DOCUMENTED page-local literal exception: cream legibility scrim over the
     watercolor (real-image scrim, not a theme color — cf. --shadow-dropdown). */
  .shero-scrim {
    position: absolute;
    inset: 0;
    background: linear-gradient(
      180deg,
      rgba(246, 243, 237, 0.72) 0%,
      rgba(246, 243, 237, 0.28) 40%,
      rgba(246, 243, 237, 0.92) 100%
    );
  }
  .shero-content {
    position: relative;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    text-align: center;
    width: 100%;
    padding-top: 72px;
    padding-bottom: 72px;
  }
  .shero-eyebrow {
    margin-bottom: 40px;
    display: flex;
    justify-content: center;
  }
  /* Keep the city/state an unbreakable unit at every width, so if the eyebrow
     has to wrap the break falls cleanly after the "·" and "Wake Forest, NC"
     never splits (no orphaned "NC"). */
  .shero-eyebrow-place {
    white-space: nowrap;
  }
  .shero-title {
    font-weight: 400;
    font-size: clamp(46px, 7.5vw, 120px);
    line-height: 1.02;
    letter-spacing: -0.02em;
    color: var(--ink);
    margin: 0;
    max-width: 16ch;
    text-wrap: balance;
  }
  .shero-lede {
    font-weight: 400;
    font-size: clamp(19px, 2vw, 25px);
    line-height: 1.6;
    color: var(--ink-2);
    margin: 32px auto 0;
    max-width: 40ch;
  }
  .shero-actions {
    display: flex;
    gap: 12px;
    justify-content: center;
    margin-top: 40px;
    flex-wrap: wrap;
  }

  /* B) Transition line */
  .shero-transition-sec {
    padding-top: 72px;
    padding-bottom: 0;
  }
  .shero-transition {
    text-align: center;
  }
  .shero-rule {
    width: 1px;
    height: 48px;
    background: var(--line-strong);
    margin: 0 auto 28px;
  }
  .shero-transition-line {
    font-style: italic;
    font-weight: 400;
    font-size: clamp(24px, 3vw, 38px);
    line-height: 1.35;
    letter-spacing: -0.015em;
    color: var(--ink);
    max-width: 24ch;
    margin: 0 auto;
    text-wrap: balance;
  }

  /* C) People photo */
  .shero-people-sec {
    padding-top: 56px;
    padding-bottom: 0;
  }
  .shero-people {
    position: relative;
    width: 100%;
    overflow: hidden;
  }
  .shero-people-photo {
    aspect-ratio: 21 / 9;
    width: 100%;
    /* TODO: real Sunday-gathering photo dropped into site/public/home-hero.jpg
       later; --bg-2 keeps the frame legible until then. */
    background-image: url("/home-hero.jpg");
    background-size: cover;
    background-position: center 35%;
    background-color: var(--bg-2);
  }
  /* Quiet placeholder marker (over the light --bg-2 fallback; token colors).
     Deleted with the TODO block in the markup once the photo lands. */
  .shero-people-pending {
    position: absolute;
    top: 18px;
    right: 22px;
    z-index: 2;
    font-family: var(--mono);
    font-size: 11px;
    letter-spacing: 0.05em;
    text-transform: uppercase;
    color: var(--ink-3);
    background: color-mix(in srgb, var(--bg) 90%, transparent);
    border: 1px solid var(--line);
    padding: 5px 10px;
    border-radius: var(--radius-sm);
  }
  /* DOCUMENTED page-local literal exception: photo-darkening caption gradient +
     white-on-photo caption text (not theme colors). */
  .shero-people-caption {
    position: absolute;
    left: 0;
    right: 0;
    bottom: 0;
    padding: 40px 40px 28px;
    background: linear-gradient(180deg, rgba(0, 0, 0, 0) 0%, rgba(0, 0, 0, 0.55) 100%);
  }
  .shero-people-caption-text {
    font-style: italic;
    font-size: clamp(18px, 1.8vw, 24px);
    color: rgba(255, 255, 255, 0.95);
    max-width: 30ch;
  }
  @media (max-width: 720px) {
    .shero-people-caption {
      padding: 24px 22px 20px;
    }
    /* Phone eyebrow — same fix as the kids hero (hero-pilot mobile pass): drop
       flex-centering back to inline/block flow so the accent dot stays attached
       to the first word instead of orphaning onto its own line, and ease the
       size/tracking down slightly (eyebrows are micro-type, exempt from the 16px
       floor) so the label fits and any wrap is the clean post-"·" break. */
    .shero-eyebrow {
      display: block;
      text-align: center;
      font-size: 12px;
      letter-spacing: 0.14em;
    }
  }

  /* ------------------------------ SERVICE TIMES ----------------------------- */
  .service {
    padding: 80px 0;
  }
  .service-band {
    display: grid;
    grid-template-columns: auto 1fr auto;
    align-items: center;
    gap: 56px;
    padding: 32px 0;
    border-top: 1px solid var(--line-strong);
    border-bottom: 1px solid var(--line-strong);
  }
  .service-eyebrow {
    margin-bottom: 6px;
  }
  .service-time {
    font-weight: 500;
  }
  .service-time .sep-dot {
    color: var(--ink-3);
  }
  .service-loc {
    border-left: 1px solid var(--line);
    padding-left: 56px;
  }
  .service-addr {
    font-weight: 500;
  }
  .service-note {
    margin-top: 4px;
  }

  @media (max-width: 720px) {
    .service-band {
      grid-template-columns: 1fr;
      gap: 28px;
    }
    .service-loc {
      border-left: 0;
      padding-left: 0;
    }
  }

  /* ----------------------- SHARED: eyebrow dot + placeholder ---------------- */
  /* Section eyebrow + its accent dot (global .eyebrow .dot only sets color). */
  .head-eyebrow {
    margin-bottom: 18px;
    color: var(--ink-3);
  }
  .head-eyebrow .dot {
    display: inline-block;
    width: 6px;
    height: 6px;
    border-radius: 50%;
    background: var(--accent);
    margin-right: 10px;
    transform: translateY(-2px);
  }

  /* .ph / .ph-label / .ph-dark moved to global.css (shared across pages). */

  /* ------------------------------ MISSION TRIAD ---------------------------- */
  .mission-head {
    max-width: 14ch;
  }
  .mission .right {
    color: var(--ink-2);
  }
  .mission-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 32px;
  }
  .mission-item {
    display: flex;
    flex-direction: column;
    gap: 20px;
  }
  /* Fills the former .ph slot: same 4/5 portrait proportion + radius, image
     cover-cropped. Replaces the striped placeholder now that photos exist. */
  .mission-photo {
    width: 100%;
    aspect-ratio: 4 / 5;
    object-fit: cover;
    border-radius: var(--radius-ph);
    display: block;
  }
  .mission-num-row {
    display: flex;
    align-items: baseline;
    gap: 14px;
  }
  .mission-num {
    color: var(--accent);
  }
  .mission-verb {
    color: var(--accent);
    font-weight: 600;
  }
  .mission-headline {
    max-width: 20ch;
    font-weight: 500;
  }
  .mission-body {
    margin: 0;
    max-width: 34ch;
    color: var(--ink-2);
  }

  /* --------------------------- PLAN-VISIT TEASER --------------------------- */
  .visit-grid {
    display: grid;
    grid-template-columns: 1fr 1.2fr;
    gap: 80px;
    align-items: start;
  }
  .visit-aside {
    position: sticky;
    top: 100px;
  }
  .visit-title {
    max-width: 12ch;
  }
  .visit-lede {
    margin-top: 28px;
  }
  .visit-actions {
    margin-top: 32px;
    display: flex;
    gap: 12px;
  }
  .visit-ph {
    margin-top: 56px;
  }
  /* Short wide welcome band (~21:9). object-position biased slightly up so the
     embracing pair reads within the short crop; no distortion (cover, not stretch). */
  .visit-photo {
    width: 100%;
    aspect-ratio: 21 / 9;
    object-fit: cover;
    object-position: center 35%;
    border-radius: var(--radius-card);
    display: block;
  }
  .visit-point {
    display: grid;
    grid-template-columns: auto 1fr;
    gap: 28px;
    padding: 32px 0;
    border-bottom: 1px solid var(--line);
  }
  .visit-k {
    color: var(--accent);
    padding-top: 4px;
  }
  .visit-h {
    font-weight: 500;
    margin-bottom: 12px;
  }
  .visit-b {
    margin: 0;
    max-width: 44ch;
    color: var(--ink-2);
  }

  /* ------------------------------ FIND COMMUNITY -------------------------- */
  .community {
    background: var(--bg-2);
  }
  .community-head {
    max-width: 16ch;
  }
  .community .right {
    color: var(--ink-2);
  }
  /* gap:1px over a --line background paints the hairline grid rules */
  /* hairline mechanism now from global .hairline-grid; only columns stay local */
  .community-grid {
    grid-template-columns: repeat(3, 1fr);
  }
  .community-card {
    background: var(--bg-2);
    padding: 36px 32px;
    display: flex;
    flex-direction: column;
    gap: 14px;
    min-height: 240px;
    color: inherit;
    transition: background 200ms ease;
  }
  /* JS onMouseEnter/Leave in the prototype → CSS :hover here */
  .community-card:hover {
    background: var(--bg);
  }
  .community-meta {
    color: var(--ink-3);
  }
  .community-name {
    font-weight: 500;
  }
  .community-body {
    margin: 0;
    color: var(--ink-3);
  }
  .community-cta {
    margin-top: auto;
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding-top: 16px;
  }
  .community-card .label {
    font-weight: 500;
    color: var(--accent);
  }
  .community-card .row-arr {
    color: var(--accent);
  }
  .community-all {
    text-align: center;
    margin-top: 64px;
  }

  /* ------------------------------ LATEST SERMON --------------------------- */
  .latest-head {
    max-width: 16ch;
  }
  .latest .right {
    color: var(--ink-3);
  }
  .sermon-card {
    display: grid;
    grid-template-columns: 1fr 1.6fr;
    border: 1px solid var(--line-strong);
    color: inherit;
    transition: background 200ms ease, border-color 200ms ease;
  }
  /* JS hover in the prototype → CSS :hover */
  .sermon-card:hover {
    background: var(--bg-2);
  }
  .sermon-art {
    aspect-ratio: 1 / 1;
    background: var(--bg-2);
    position: relative;
    display: flex;
    align-items: center;
    justify-content: center;
    border-right: 1px solid var(--line-strong);
  }
  /* Fills the square series-artwork tile; center-cropped from the 3:2 source. */
  .sermon-art-img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
  }
  .sermon-art-tl {
    position: absolute;
    top: 18px;
    left: 18px;
    color: var(--ink-3);
  }
  .sermon-art-title {
    font-family: var(--display);
    font-size: 56px;
    font-weight: 500;
    letter-spacing: -0.04em;
    color: var(--ink);
    line-height: 0.95;
    text-align: center;
  }
  .sermon-art-bl {
    position: absolute;
    bottom: 18px;
    left: 18px;
    color: var(--accent);
  }
  .sermon-body {
    padding: 44px 48px;
    display: flex;
    flex-direction: column;
    gap: 16px;
  }
  .sermon-kicker {
    color: var(--ink-3);
  }
  .sermon-title {
    max-width: 16ch;
    margin: 0;
  }
  .sermon-lede {
    margin: 0;
    font-size: 17px;
    color: var(--ink-2);
    max-width: 52ch;
    line-height: 1.65;
  }
  .sermon-foot {
    margin-top: 20px;
    padding-top: 24px;
    border-top: 1px solid var(--line);
    display: flex;
    gap: 16px;
    align-items: center;
    justify-content: space-between;
  }
  .sermon-listen {
    color: var(--accent);
  }

  /* --------------------------- CARE (dark band) --------------------------- */
  /* Dark band uses INVERSE semantic tokens (per design-tokens.md) — no inline
     white rgba. Faint card fills use color-mix over --ink-on-inverse so even
     the surface tints stay token-derived. */
  .care {
    background: var(--bg-inverse);
    color: var(--ink-on-inverse);
  }
  .care-eyebrow {
    color: var(--ink-on-inverse-2);
  }
  .care-title {
    max-width: 16ch;
  }
  .care-note {
    margin: 0;
    color: var(--ink-on-inverse-2);
  }
  .care-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 24px;
  }
  .care-card {
    padding: 32px 28px 28px;
    border: 1px solid var(--line-on-inverse);
    border-radius: var(--radius-card);
    display: flex;
    flex-direction: column;
    gap: 14px;
    min-height: 280px;
    color: inherit;
    background: color-mix(in srgb, var(--ink-on-inverse) 3%, transparent);
    transition: background 200ms ease, border-color 200ms ease;
  }
  /* JS onMouseEnter/Leave in the prototype → CSS :hover */
  .care-card:hover {
    background: color-mix(in srgb, var(--ink-on-inverse) 6%, transparent);
    border-color: var(--border-on-inverse);
  }
  .care-tag {
    color: var(--accent-on-inverse);
  }
  .care-name {
    margin-top: 4px;
    font-weight: 500;
  }
  .care-body {
    margin: 0;
    line-height: 1.55;
    color: var(--ink-on-inverse-2);
  }
  .care-cta {
    margin-top: auto;
    padding-top: 20px;
    font-size: 15px; /* CTA text row — 15px floor at all viewports */
    color: var(--accent-on-inverse);
    display: flex;
    align-items: center;
    justify-content: space-between;
  }

  /* ------------------------- EVENTS + WHO IS JESUS ------------------------ */
  .events-grid {
    display: grid;
    grid-template-columns: 1.2fr 1fr;
    gap: 80px;
  }
  .events-title {
    margin-bottom: 40px;
  }
  .event-left {
    display: flex;
    align-items: center;
    gap: 32px;
  }
  .event-date {
    width: 64px;
  }
  .event-month {
    color: var(--accent);
  }
  .event-day {
    font-weight: 500;
    line-height: 1;
  }
  .event-name {
    font-weight: 500;
    margin-bottom: 4px;
  }
  .event-arr {
    color: var(--accent);
  }
  .events-actions {
    margin-top: 32px;
  }
  .events-empty {
    margin: 0;
    font-size: 16px;
    color: var(--ink-3);
  }

  /* Accent band: white-on-accent. Solid white → --accent-ink; partial-opacity
     whites are expressed as color-mix over --accent-ink (token-derived, no raw
     white rgba). */
  .jesus-aside {
    background: var(--accent);
    color: var(--accent-ink);
    padding: 56px 48px;
    border-radius: var(--radius-card);
    display: flex;
    flex-direction: column;
  }
  .jesus-eyebrow {
    color: color-mix(in srgb, var(--accent-ink) 70%, transparent);
  }
  .jesus-eyebrow .dot {
    background: var(--accent-ink);
  }
  .jesus-title {
    color: var(--accent-ink);
    max-width: 10ch;
    margin-bottom: 24px;
  }
  .jesus-body {
    margin: 0 0 28px;
    color: color-mix(in srgb, var(--accent-ink) 86%, transparent);
  }
  .jesus-actions {
    margin-top: auto;
  }
  .jesus-btn {
    background: var(--accent-ink);
    color: var(--accent);
  }

  /* Mobile collapses — enhancement beyond the prototype (it had no mobile
     rules for these grids); prevents unusable multi-column layouts on phones. */
  @media (max-width: 880px) {
    .mission-grid,
    .community-grid,
    .care-grid {
      grid-template-columns: 1fr 1fr;
    }
  }
  @media (max-width: 720px) {
    .mission-grid,
    .community-grid,
    .care-grid {
      grid-template-columns: 1fr;
    }
    .visit-grid {
      grid-template-columns: 1fr;
      gap: 40px;
    }
    .visit-aside {
      position: static;
    }
    .sermon-card {
      grid-template-columns: 1fr;
    }
    .sermon-art {
      border-right: 0;
      border-bottom: 1px solid var(--line-strong);
    }
    .events-grid {
      grid-template-columns: 1fr;
      gap: 48px;
    }
  }
</style>
