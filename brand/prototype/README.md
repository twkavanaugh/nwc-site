Design prototype reference (Claude Design React/Babel export). Source of truth for visual fidelity and content. NOT part of the Astro build — do not import. The Astro site rebuilds these as static .astro routes consuming semantic tokens. Prototype-only machinery (tweaks-panel, hash routing, onNav, Google Fonts CDN, the PALETTES object, [data-theme=dim]) must NOT carry into the build.
